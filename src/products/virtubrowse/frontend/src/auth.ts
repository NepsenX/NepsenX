export const login = async (credentials: any) => {
  // SSO login logic using core auth
};

export const logout = () => {
  // Logout logic
};

export const getCurrentUser = () => {
  // Session check
};
